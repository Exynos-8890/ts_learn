// 第二章练习：控制流与比较
// 把 ___ 替换成正确实现。
import { __ as ___ } from "./_placeholder.js";

// 1) 用严格等于判断两个值是否完全相等（不做类型转换）
export const isSame = (a, b) => ___; // 使用 === 或 !==

// 2) 返回分数等级：>=90 A，>=80 B，否则 C
export function grade(score) {
  return ___;
}

// 3) 用三元表达式返回奇偶：偶数 "even"，奇数 "odd"
export const parity = (n) => ___;

// 4) 用 switch 把 1 -> "Mon"，2 -> "Tue"，其他 -> "Other"
export function toWeekday(n) {
  return ___;
}
