// 第一章练习：变量与类型
// 任务：把所有的 ___ 替换成正确的 JS 代码或字面量。
// 目标：理解 const/let、number/string/boolean、undefined/null 的区别。
import { __ as ___ } from "./_placeholder.js";

// 1) 用 const 定义一个圆周率 pi，并赋值为 3.14（保留两位足够）
export const pi = ___; // e.g. 3.14

// 2) 用 let 定义一个可变计数器 counter，初始为 0，然后将它加 1
export let counter = ___; // e.g. 0
counter = ___; // e.g. counter + 1

// 3) 声明一个变量 a 但不赋值（应为 undefined）
export let a = ___; // 提示：不要给具体值，可用 undefined

// 4) 显式声明一个变量 b 并赋值为 null
export const b = ___; // e.g. null

// 5) 把 x 从 number 改为 string（动态类型）
export let x = 42;
x = ___; // 把 x 改成字符串 "42"，例如 "42"

// 6) 定义一个布尔量 isJSFun，并赋值为 true
export const isJSFun = ___; // e.g. true
