// 第四章练习：数组与对象
import { __ as ___ } from "./_placeholder.js";

// 1) 把数组 [1,2,3] 每个元素 +1，得到 [2,3,4]
export const plusOne = (arr) => ___;

// 2) 过滤出偶数
export const evens = (arr) => ___;

// 3) 累加求和
export const sum = (arr) => ___;

// 4) 读取对象属性 name
export const getName = (user) => ___;

// 5) 返回一个新对象，age + 1（不修改原对象）
export const birthday = (user) => ___;
