// 第五章练习：循环
import { __ as ___ } from "./_placeholder.js";

// 1) 用 for 循环计算 1..n 的和
export function sumTo(n) {
  return ___;
}

// 2) 用 for...of 复制一个数组
export function copy(arr) {
  return ___;
}

// 3) 用 while 统计数组中 >0 的元素个数
export function countPositive(arr) {
  return ___;
}
